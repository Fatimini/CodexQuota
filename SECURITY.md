# 安全政策

## 支持范围

本项目只维护**默认分支（`main`）**上的最新源码。
不维护历史版本，也不维护任何未经签名的第三方修改版。

## 如何报告

请**不要**在公开 Issue 中贴出本机环境信息。提交前请先移除或打码：

- 账号邮箱、套餐类型、额度的原始 JSON 响应
- `~/.codex/` 下的任何内容
- `~/Library/Application Support/CodexQuota/history.json` 的内容
- 绝对路径、用户名、主机名、CLI 版本之外的环境细节

优先使用本仓库的 **Security Advisories**（私密报告渠道，默认分支已启用）。
若 Advisories 不可用，请只提交脱敏后的问题描述，不要附带日志原文。

## 本项目的自我约束

- 不读取 `~/.codex/auth.json`、浏览器 Cookie、API Key 或 Session Token
- 不上传任何数据：无遥测、无云端服务
- 除本机历史曲线文件外不写盘（见 README「隐私」章节）

## 不属于安全漏洞的常见误报

- **ad-hoc 签名导致 Gatekeeper 拒绝启动**：分发形态限制，不是漏洞（见 README「安装与构建」）
- **CLI 未安装或升级后读取失败**：兼容性问题，不是漏洞（见 README「兼容性」）
- **额度显示与网页端不一致**：两者是不同的限额桶，且快照有 3 分钟粒度，属预期行为
